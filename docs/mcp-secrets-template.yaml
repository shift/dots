# MCP Servers Secrets Template
#
# Environment Variables for MCP Servers
# 
# Note: SOPS integration has been removed from the home-manager MCP modules
# to avoid build compatibility issues. You can either:
#
# 1. Set environment variables directly in your shell configuration:
#    Add to ~/.zshrc, ~/.bashrc, or your shell config:
#    export GITHUB_PERSONAL_ACCESS_TOKEN="ghp_your_github_token_here"
#    export BRAVE_API_KEY="your_brave_search_api_key_here" 
#    export POSTGRES_CONNECTION_STRING="postgresql://user:password@localhost:5432/dbname"
#
# 2. Use system-level SOPS configuration (advanced):
#    Add these secrets to your secrets/common.yaml or secrets/[hostname]/secrets.yaml
#    and configure them in your NixOS configuration (not home-manager).
#
# Example entries for SOPS (if configuring at system level):
# github_personal_access_token: "ghp_your_github_token_here"
# brave_api_key: "your_brave_search_api_key_here" 
# postgres_connection_string: "postgresql://user:password@localhost:5432/dbname"